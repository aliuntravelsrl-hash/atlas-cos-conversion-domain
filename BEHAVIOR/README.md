# Behavior

Describe el comportamiento del dominio independientemente del agente que lo ejecute.

Principio:

> El núcleo no cambia; cambia el conocimiento.

Cada punto se analizará como:

```text
RECIBE → HACE → ENTREGA → EVIDENCIA → SIGUIENTE CONTEXTO
```
